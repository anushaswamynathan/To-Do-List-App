services:
  - type: web
    name: my-to-dos
    runtime: python
    plan: starter
    buildCommand: ""
    startCommand: NIGHTLY_TODOS_HOST=0.0.0.0 NIGHTLY_TODOS_DATA_DIR=/var/data python3 server.py
    disk:
      name: my-to-dos-data
      mountPath: /var/data
      sizeGB: 1
